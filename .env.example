# Copier vers .env si besoin (chargé manuellement : `set -a && source .env && set +a`).
# ./scripts/dev exporte déjà cette valeur par défaut.
DATABASE_URL=postgresql+psycopg://doux:doux@127.0.0.1:5432/doux_planning
