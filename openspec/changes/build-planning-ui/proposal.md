## Why

Le moteur et le contrat d’exemple existent (`GET /v1/examples/saint-cloud`), mais le restaurateur n’a encore aucune application pour *voir* le cycle. Un rapport HTML et un canvas IDE ne sont pas le produit. Il faut un client React qui affiche le snapshot tel que le moteur l’a déjà scoré.

## What Changes

- Ajouter une SPA React + TypeScript (Vite) : premier écran utile = grille papier 14 jours (semaines A / B) + liste des warnings, à partir du JSON exemple.
- Afficher aussi les compteurs `planning.stats`, le tableau légal (`legal` + `legal_rows`) et le tableau souhaits (`wish_cols` + `wish_rows`) — forme de [la démo](https://1mbc.github.io/doux-planning/) et du canvas `exemple-restau-planning`, pas une copie du moteur.
- Consommer `GET /v1/examples/saint-cloud` pour la grille. Aucune génération, aucun score, aucune décision dans le front.
- Écrans **connexion** et **inscription** (`/register`) selon `contracts/http/v1-auth.md` : un login (email + mot de passe, `kind` depuis `me`) ; register Entreprise / Salarié (pas « restaurateur ») ; QR = même register + query `company_code` + `employee_token`. Pas de « mot de passe oublié ». Password ≥ 8. Afficher `detail` API tel quel.
- Token en `sessionStorage`, header `Authorization: Bearer` uniquement sur register/login/logout/`GET /v1/me`. **Pas** sur `/v1/examples/*` ni `/v1/sandbox/*`. Sans session : auth **et** exemple Saint-Cloud restent accessibles (« Voir l’exemple »). Ne pas bloquer la grille derrière le login.
- `kind: employee` : pas de Mode édition, pas de wizard `/context` (403 API). `parseMe` : `restaurant_id: string | null` (affilié = les deux strings ; sans affiliation = les deux `null`). Affilié : route `/planning` (lien chrome « Planning ») selon `contracts/http/v1-me-planning.md` : `GET /v1/me/planning` Bearer, grille équipe, highlight de ses lignes, panneau contrat / indispos / souhaits lecture seule. Pas de Calculer ni live sandbox. **Sans affiliation** (`employee_id === null`) : **pas** `/planning`, pas de lien « Planning » ; écran code entreprise → `GET /v1/invites/{code}` → `POST /v1/auth/link` ; succès → `/planning`.
- `kind: company` : route `/context` (lien « Mon restaurant ») — wizard selon `contracts/domain/wizard-ui.md` : **Services → Rôles → Équipe → Souhaits → Services types → Semaine type**. Rôles : `<table>` Nom / Niveau / poubelle. **Équipe** : poubelle par fiche (même chrome) ; confirm FR = `contracts/domain/delete-employee.md` (fiche + indispos / souhaits ; s’il a un compte : accès retiré, reconnexion au code ; planning publié **de cette équipe** jeté, l’autre intacte). Ligne jamais PATCH : retrait local. Persistée : `DELETE /v1/staff/{id}` puis GET context. **Min. créneau** : un `Stepper` par service offert (`CONTEXT_SERVICES.filter`, PDJ → déj → dîner), défaut 4, `step={0.5}` `min={0.5}`, `min_shift_hours` objet sparse (`min-shift-per-service.md` gagne) ; plus l’`<input type="number">` unique. Affichage des services offerts (sous-onglets types **et** colonnes semaine type) : `CONTEXT_SERVICES.filter(s => offered.includes(s.id))` (Petit-déjeuner → Déjeuner → Dîner), **jamais** `services.map` ; persist `services[]` inchangé. Services types : **une `<table>` par feuille**, colonnes Type (**Arrivée** | **Départ**) · Heure (cadran overlay + ±15) · **Niveaux minimal requis (par arrivée | après départ)** · STAFF (plus de colonne N). Ajouter arrivée/départ = cadran **avant** d’insérer (11h00 / 16h00 ; Annuler / Escape / backdrop = pas de ligne). Réordre chrono animé ≥ 500 ms + fond focus. Stepper **encadré**, libellé gras, chiffre centré. Invite : **code entreprise** + URL **absolue** + QR. Copies Équipe / Souhaits / types / Semaine type retirées. `weekend_rest_day` en **colonne** à part. Bearer sur GET/PATCH `/v1/context`, `DELETE /v1/staff/{id}` et `POST /v1/context/seed-example`.
- `kind: company` : bouton **Intégrer l’exemple Saint-Cloud** sur `/context` (bandeau, à côté du code). Tous les comptes restaurateur. Confirm FR puis `POST /v1/context/seed-example` (Bearer, pas de body). 200 = même `Context` que GET. Pas de generate, pas d’aller sur `/planning`. Pas de bouton salarié / exemple / planning / login.
- `kind: company` : **Exporter la config** / **Importer une config** sur la même `seed-row` (`contracts/domain/export-config.md` § UI). Export = `GET /v1/context/export` → JSON `export_version === 1` (pas de token). Import = fichier `.json` + confirm FR → `POST /v1/context/import` → `adopt` comme le seed. Pas de bouton ailleurs. Pas d’exports planning.
- `kind: company` : route `/planning` selon `v1-generate.md` + `generate-versions.md` + `generate-jobs.md` + `cycle-recaps.md` + `score.md` + `score-facts.md` + **`manual-planning.md` (gagne)** : **3 rangées** — Salle/Cuisine ; Minimal/Optimisé/Maximal/**Manuel** = **sélection** (défaut `latest`) ; actions blanches **(Re)Calculer le planning** (computes seulement) / **Entrer en mode édition** / **Quitter le mode édition** / **Publier** / **Exporter**. Recalculer = POST de l’effort compute sélectionné — **jamais** `postGenerate(..., "manuel")`. Cran **Manuel** : pas (Re)Calculer ; `ready[team]` → édition même si `versions.manuel` null ; slot vide hors édition → « Pas encore publié » ; enter `{ search_effort: "manuel" }` ; mêmes Overlay / Fill / undo / discard / publish. Computes : slot vide → « Pas encore calculé » ; édition ssi cycle non null. Sous la rangée 3 : `generated_at` Europe/Paris ; cran manuel : pas de ` · engine_ref`, durée `—` si absente. Rangée **notes /10** : **Globale à gauche** (cadre contrasté, pas de résumé) puis 5 axes ; pastille **titre gras** puis **note + jauge même ligne** puis totaux ; **Contrat /10** ; **clic** → tableau 4 col miss groupés puis hits ; HSL `hue = 12 × note`. Plus de cartes stats. Après Alertes : **Légal & Contrat** puis **Bien-être**. Loader ≥ 1 s. Maximal = 202 + poll. Parse `published[team].versions` + `latest` + `facts[]` ; 4ᵉ clé `manuel` absente → `null`. Salarié : `me/planning` `latest` seulement, pas de sélecteur ni notes. Banc : `BENCH_EFFORTS` reste 3 ; parsers banc rejettent `manuel`.
- `kind: company` : menu **Exporter** (JSON / CSV / XLSX / JPEG) dans `planning-actions` selon `contracts/domain/export-planning.md`. Côté client, équipe courante, cycle déjà chargé. Actif ssi `published[team]` et pas en édition. JSON / CSV inchangés. XLSX = 2 feuilles Semaine A / B (ou Paire / Impaire), titre daté, une ligne par service offert. JPEG = 2× pixels, qualité ≥ 0.95, les 2 semaines. Pas de nouvelle route. Pas de bouton salarié / `/exemple`.
- `/exemple` lit le snapshot **facts** (`score-facts.md`) : 92 shifts, **17** alertes en tableau 4 col, clic Contrat = miss puis hit, Diane `⚠️ 30h · 29h / 39h`, **Légal & Contrat** + **Bien-être**. Chrome notes /10.
- `kind: company` : **Mode édition** sur `/planning` si `published[team]` existe — `POST /v1/live/sandbox/{team}/enter` (Bearer) selon `contracts/http/v1-live-sandbox.md`. Même overlays que le joujou (retune ±15 Valider, replace, swap, fill). Lecture sans discard. Publier → `POST .../publish` puis grille = `GET /v1/cycles`. Zéro appel `/v1/sandbox/*` depuis `/planning`. `/exemple` joujou inchangé (public).
- `me.admin` : `/admin` table selon **`admin-historique.md` (gagne)** : colonnes **Note** (`formatCycleNote(score_global)`, `8,4` / `—`) et **Planning** (boutons **Voir** + **Au banc**, Voir inactif si `restaurant_id` null). Parser `restaurant_id` / `score_global` (`string | null` / `number | null` ; clés absentes → null, pas de crash). Clic droit **email** : `preventDefault`, `POST /v1/admin/impersonate` `{ restaurant_id }`, copie `url` (`navigator.clipboard.writeText`, fallback ok), toast « Lien copié — ouvre-le en navigation privée. » ; resto introuvable → toast « Restaurant introuvable. », pas de POST. **Jamais** `window.open` / auto-navigation. Clic **Voir** → `go("/admin/planning/" + restaurant_id)`. Clic droit **nom restaurant** et bouton **Au banc** : popup import (`bench-import.md`). `/admin/planning/{id}` : `me.admin` seulement (sinon message réservé, 0 fetch) ; GET `/v1/admin/restaurants/{id}/cycles` + `/context` ; reprend **PublishedPlanning lecture seule** (4 crans, recaps, export client ; **pas** (Re)Calculer, **pas** édition live, **pas** overlay). Titre = nom du resto. Menu admin. 404 → `Restaurant introuvable.` Route publique `/impersonate/{token}` (sans `me.admin`, `me` null OK, ne pas tomber sur Login avant consume) : `POST /v1/auth/impersonate` `{ token }` sans Bearer ; 200 → `sessionStorage` (`AUTH_TOKEN_KEY`) + `go("/planning")` ; échec → `detail`. Hover warnings / en-têtes jour / sélecteur moteur inchangés. File 70 inchangé.
- `me.admin` : popup **Au banc** selon **`bench-import.md` (gagne)** : clic droit **nom restaurant** (pas l’email) **et** bouton **Au banc** à côté de Voir. `restaurant_id` null → pas de popup, toast `Restaurant introuvable.` En-tête nom + email. GET `/v1/admin/restaurants/{id}/import-preview` : encadré **complet ?** (prêt / manuel / computes par équipe, `generate_count`) ; erreur HTTP → `detail` dans le popup, pas de crash. 4 cases **ON** : Salle, Cuisine, Dernier planning manuel publié, Computes déjà publiés. Note manuel /10 optionnelle (vide = `manual_score: null`). Commentaire textarea optionnel (vide = `comment: null`). Annuler / **Importer**. POST `/v1/admin/bench/import` envoie les 4 bools explicites. Succès : fermer, toast « Jeu importé. » + bouton vers `/admin/bench`. Erreur POST → `detail`. Parser `GET /v1/admin/bench/versions` : `origin` (`catalogue` | `imported`, absent → `catalogue`) + `comment` (absent → `null`). File 70 (Note / Voir / clic droit email) inchangé. File 71 popup inchangé.
- `me.admin` (bool, employee → `false`) : lien **Admin** dans le chrome ssi `true`. Routes `/admin`, `/admin/bench`, `/admin/bench/run/{run_id}` et compare-chemin : menu **Historique des computes | Banc** (entrée courante marquée). `/admin/bench/versions` redirige vers `/admin/bench`. `/admin` = log generate **plus** Note / Voir / Au banc / lien impersonate (ci-dessus). `/admin/bench` selon **`bench-chrome.md` (gagne)** + `bench.md` + `bench-import.md` : lancer 2 lignes (toutes catégories × 3 efforts ; 3 dropdowns d’effort → catégories, `scope=category`) ; **Compléter les trous** (`scope=gaps`) ; loader batch `%` + `eta_max_seconds` **inline sous Lancer** (pas d’overlay Banc) ; recap `{ref} vs {prev}` ; tableau + jeux API (catalogue ∪ `imported`) ; **un** Manuel à gauche puis **une colonne** par `engine_ref` (3 computes empilés) ; cellules = delta coloré. **Plus** de colonnes **Défi** / **Lancer**. **Exporter sous le Manuel** + **Exporter tout le banc** dans le bloc Lancer. Sous Derniers runs : filtre **Tous | IA | Manuels**. Menu `…` : 3 lancers, **Exporter ce jeu** `scope=dataset`, **Supprimer…** (`DELETE` datasets). Stats banc inchangée. Non-admin : message habituel, 0 appel bench. Vide generate → « Aucun generate pour l’instant. » Tableau filtré vide → « Aucun jeu. »
- UX en français. Les messages moteur anglais sont présentés, pas recalculés.
- Wizard `/context` : rôles en `<table>` Nom / Niveau / poubelle (confirm FR + fiches + conseiller de renommer) ; copies Équipe / Souhaits / Services types / Semaine type retirées ; invite affiche + copie le `company_code`.
- Ne pas archiver ni synchroniser. Ne pas modifier `src/doux_planning/`, `contracts/`, Compose, Alembic.

## Capabilities

### New Capabilities

- `planning-ui`: application web du snapshot d’exemple, login / register / session, wizard contexte, cycle publié (Calculer), édition live du cycle, et grille salarié.

### Modified Capabilities

- (aucune — le moteur et ses exigences restent dans `define-planning-core`)

## Impact

Uniquement `web/`. Proxy Vite `/v1` inchangé. API = uvicorn `master` + worker Compose. Version `0.58.0`. Hors slice : rotate invite-token, panneau compte salarié, unlink sans delete fiche, édition des poids, rewrite `saint-cloud.json`, `SERVICE_ROWS` Matin/Soir du joujou `/exemple`, overlay sandbox `/exemple`. File 70 (Note / Voir / clic droit email) et file 71 (popup Au banc) inchangés. Pas de chrome worker. Pas de nouvelle dépendance npm. Pas d’archive / sync. Pas de Core / Infra. Ne pas modifier `contracts/`.
