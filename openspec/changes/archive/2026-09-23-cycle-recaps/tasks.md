## 1. Recap and 11 h clocks

- [x] 1.1 Add `cycle_recap` (stats / legal / wish from the published result) and the French `rest_between_days` clocks, and verify the freeze salle-recap, weekend_rest_day column, no-`we1j`, and 11 h message scenarios

## 2. Guardrails

- [x] 2.1 Run domain / engine / context / board / hydrate pytest green without rewriting `saint-cloud.json` and without editing `web/`, `api/`, or `contracts/`
