## 1. Snapshot recap

- [x] 1.1 Refresh Saint-Cloud `planning.warnings` / `stats` / `legal_rows` / `wish_cols` / `wish_rows` from `evaluate` + salle `cycle_recap` without `generate_cycle`, and verify 92 assignments, Théo 11h–16h, Diane contrat, no `we1j` / `weA`, and FR `contrat` / `pas deux repos` substrings

## 2. Guardrails

- [x] 2.1 Run hydrate / seed / domain pytest green without editing `web/`, `api/`, or `contracts/` and without changing `restaurant` or `assignments` values
