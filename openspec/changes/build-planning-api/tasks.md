## 1. Example contract and seed

- [x] 1.1 Add PostgreSQL 16 + Alembic + SQLAlchemy 2 + psycopg to the API stack (`pyproject.toml`, `docker-compose.yml` with `db` / `api`), and verify `docker compose up` reaches a healthy Postgres and the API process starts with `DATABASE_URL`
- [x] 1.2 Create migrations for `legal_contexts`, `restaurants` (`legal_context` id, no copied `legal_rules`), and `example_snapshots` (frozen `planning` jsonb), and verify `alembic upgrade head` succeeds on an empty database
- [x] 1.3 Seed France from `data/legal/france.json` and Saint-Cloud from `data/examples/saint-cloud.json` at boot or migrate, and verify the restaurant row stores `legal_context = france` with no `legal_rules` column/document
- [x] 1.4 Point `GET /v1/examples/{example_id}` at the live store (not a per-request file read, not `generate_cycle`, not a job) inside `src/doux_planning/api/` only, and verify `TestClient` `GET /v1/examples/saint-cloud` is 200 with `example`, `legal.id == france`, `legal.kind == legal_context`, no `restaurant.legal_rules`, `planning.search_effort == optimized`, `planning.stats.assignments == 70`, and `GET /v1/examples/inconnu` is 404 (extend `tests/test_planning.py`)

## 2. Auth HTTP (unified contract)

- [x] 2.1 Add Argon2 password hashes plus live company/fiche tables, `restaurateur_accounts`, `employee_accounts`, and hashed `sessions` (opaque Bearer), and verify a stored password is hashed, the raw token is not persisted, and auth without `DATABASE_URL` returns 503 while the public example dual-read is unchanged
- [x] 2.2 Implement unified `POST /v1/auth/register` and `POST /v1/auth/login` (`kind: company` creates a new empty company, not Saint-Cloud; `kind: employee` wraps `redeem_invite`). Verify company register → login → `GET /v1/me` `kind` company, duplicate email is 409, wrong password is 401, and `/v1/auth/restaurateur/*` + `/v1/auth/employee/*` are not implemented (extend TestClient tests)
- [x] 2.3 Implement public `GET /v1/invites/{company_code}` (unlinked fiches only, no tokens in JSON) and `POST /v1/staff/{id}/invite-token` (company Bearer, `rotate_employee_invite_token`). Verify two inserted fiches appear in invites, manual register on A is 201 then gone from invites, same fiche is 409, QR token B links B, bad `company_code` is 400, and rotate A rejects the old token then accepts the new one
- [x] 2.4 Implement `POST /v1/auth/logout` and `GET /v1/me`, and verify logout then `/me` is 401, `GET /v1/examples/saint-cloud` stays 200 / 92 assignments with and without a session, and existing sandbox routes stay 200 without Bearer

## 3. Live context persist (`GET` / `PATCH /v1/context`)

- [x] 3.1 Extend `companies` / `staff_fiches` (legal_context_id, services, ladders, types, typical week, contract / unavail / wellbeing / min_shift / role level) and implement `GET /v1/context` (Bearer company, wrap `empty_restaurant` / `team_ready`, no `generate_cycle`). Verify register company then GET is empty (name `""`, services `[]`, ladders/types/week empty, ready false, `legal_context_id` `france`, `company_code` = invite), employee Bearer is 403, and without `DATABASE_URL` context is 503
- [x] 3.2 Implement `PATCH /v1/context` (optional section replace) and persist restart-safe. Verify PATCH name + salle ladder + 1 fiche + services + type + open week → `ready.salle` true / `ready.cuisine` false; `reset_engine` then same GET; open cell without type → salle false; new fiches get a Core `invite_token`; rotate stays `POST /v1/staff/{id}/invite-token`
- [x] 3.3 Verify PATCH does not write `example_snapshots` or `data/examples/saint-cloud.json`, `GET /v1/examples/saint-cloud` stays 92 assignments, `GET /v1/invites/{company_code}` lists the unlinked fiche, and auth login/logout stay green
- [x] 3.4 Wrap Core `Wellbeing` / `Unavailability(weekday, service_id)` / `week_label_scheme` / `BoardWish` on `GET`|`PATCH /v1/context` and `GET /v1/me/planning`. Persist `staff_fiches.wellbeing` as a JSON object. Verify PATCH `weekend: "even"` → GET `week_labels: "parity"`; `every_two` alone → `"ab"`; indispo without `service_id` → 400; wishes have `kind` not `key`; example stays 92 / 17 / 10/12 / 47; auth / context / cycles / me/planning and dual-read without DB stay green. No `WellbeingPreference`
- [x] 3.5 Implement `POST /v1/context/seed-example` (Bearer company, no body) wrapping Core `seed_example_context`. Persist services, hours, ladders, types, typical week, example fiches (Core tokens). Smash linked fiches without 409; clear `published_cycles`, `live_sandboxes`, `linked_employee_ids`; delete this company’s employee accounts and sessions. Keep company id / name / invite / legal. Verify empty or named company → ready salle / not cuisine, midday+evening, example fiches, `week_labels` `ab`, cycles null; second seed still null cycles and new tokens; linked fiche + published cycle → 200, linked empty, old employee token 401; employee 403 / no Bearer 401 / no DB 503; public example stays 92. No `hydrate_delivered_cycle`
- [x] 3.6 Persist Core `Wellbeing.weekend_rest_day` on `staff_fiches.wellbeing` JSONB (no Alembic). Verify PATCH `true` → GET `true` after `reset_engine`; omitted key → GET `false`; legacy `at_least_one_weekend_rest_day` / key list → 400; generate + employee GET `/v1/me/planning` includes `{ kind: "weekend_rest_day", held }` when posed; seed / example stay 92 / 17 / 10/12. No coerce-on-read of old list rows
- [x] 3.7 Implement `GET /v1/context/export` and `POST /v1/context/import` (`export_version: 1`). Verify export has no `company_code` / `invite_token`; import on empty company is GET-ready from the JSON with new tokens ≠ ids; linked fiche + published cycle → 200, cycles null, linked empty, old employee 401; `export_version: 2` → 400; employee 403 / no Bearer 401 / no DB 503; example stays 92. Reuse seed smash. No Alembic. No `generate_cycle`
- [x] 3.8 Coerce-on-read + heal legacy Railway wellbeing JSONB in `api/` only (`wellbeing_codec.py` + fiche load). GET context / export / generate / me/planning map list keys, removed keys, `every_*`, `service_id` null, and fiche `max_*_per_week` to the Core object, then UPDATE the row when stored JSONB differs. Verify inserted list `two_consecutive_rest_days` / `weekend_off_every_two_weeks` / `at_least_one_weekend_rest_day` / `max_two_coupures_per_week` → GET object (`consecutive_rest`, `weekend: every_two`, `weekend_rest_day: true`, coupures 2), second GET identical, row healed; `no_evening_service` → `max_services.evening: 0`; indispo `every_morning` + journée `service_id: null` (resto midi+soir) → `morning` / `midday`+`evening` lines; PATCH / import list, removed key, or `every_*` → 400 `Champs invalides.`; already-Core PATCH/GET unchanged; example stays 92. No Alembic. No coerce on write. No Core / `web/` / `contracts/` edits

## 4. Team generate (sync HTTP)

- [x] 4.1 Persist live `published_cycles` (JSONB on the company, not `example_snapshots`) and implement `GET /v1/cycles` (Bearer company). Verify a restaurant that never generated returns `{ published: { salle: null, cuisine: null } }`, employee Bearer is 403, and without `DATABASE_URL` generate/cycles are 503 while the public example stays 92
- [x] 4.2 Implement sync `POST /v1/generate` `{ team, search_effort? }` that wraps Core `generate_team` (default effort `optimized`; tests use `minimal` only). Verify salle-ready context → POST `minimal` returns 200 with non-empty `published.salle.assignments` all `team: salle` and `published.cuisine` null; `TeamNotReady` (cuisine) is 409 French with no solve; a second POST salle replaces only salle
- [x] 4.3 Verify `reset_engine` then `GET /v1/cycles` matches the last published body, `GET /v1/examples/saint-cloud` stays 92, and context GET / auth stay green. No `jobs` table, no Compose worker, no `SKIP LOCKED`
- [x] 4.4 Persist Core `cycle_recap` on `POST /v1/generate` and `GET /v1/cycles` (and live publish). Verify salle `minimal` 200 has `stats.assignments == len(assignments)`, `legal_rows` / `wish_cols` present, no `we1j`; GET after reset matches; cuisine stays `null`; 409 if not ready; stored cycle without recap hydrates on GET; example stays 92. No Alembic. Do not recompute stats in `api/`

## 5. Synchronous adapters

- [ ] 5.1 Add restaurateur `POST /v1/evaluate`, `/v1/swap`, `/v1/rank` that call the engine and serialize `EngineResult` (no job row), and verify swap/rank/evaluate warnings match a direct engine call on the same draft
- [x] 5.2 Implement `/v1/live/sandbox/{team}` enter / GET / preview / commit / undo / discard / publish (Bearer company, wrap Core `enter_live_sandbox` / preview-apply-undo / `discard_live_sandbox` / `publish_live_sandbox`). Verify salle generated (`minimal`) → enter 200; cuisine enter is 409 French; retune + commit + undo + discard restores the published cycle; publish updates `GET /v1/cycles` salle only then GET live is 404; `reset_engine` keeps an open draft; employee 403; joujou `POST /v1/sandbox/enter` without Bearer stays 200; example stays 92. Do not change `/v1/sandbox/*`. No evaluate / swap / rank, no `/me/shifts`, no worker
- [x] 5.3 Implement `GET /v1/me/planning` (Bearer employee, wrap Core `employee_board`, not `/me/shifts`). Verify register + salle-ready PATCH + generate `minimal` + employee on fiche A → 200 with all published salle assignments, `employee_id` A, `contract` / `wishes` present, no `invite_token`; an unpublished live cran is invisible; company Bearer is 403 French; a cuisine employee with no cycle gets empty assignments; example stays 92; auth / cycles stay green. No evaluate / swap / rank

## 6. Admin promote and generate logs

- [x] 6.1 Add Alembic `is_admin` on restaurateur accounts plus `generate_logs`. Promote `ADMIN_EMAIL` at boot (existing restaurateur only, idempotent, no insert). `me.admin` true/false. Log `POST /v1/generate` 200 only. `GET /v1/admin/generates` newest-first. Verify promote twice → one account `admin: true`; unknown email → no insert; generate 200 → +1 log and 409 → no log; GET empty then two generates newest-first; non-admin company / employee → 403; example stays 92. No `kind: admin`
