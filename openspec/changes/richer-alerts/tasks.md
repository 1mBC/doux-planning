## 1. Warning and wish texts

- [x] 1.1 Enrich `empty_post`, `max_mornings` / `max_middays` / `max_evenings`, and `max_coupures` messages plus `cycle_recap` wish measures, and verify the freeze empty_post, max_evenings, and wish `max_evening` scenarios

## 2. Guardrails

- [x] 2.1 Run domain / engine / recap / board / hydrate pytest green without rewriting `saint-cloud.json` and without editing `web/`, `api/`, or `contracts/`
