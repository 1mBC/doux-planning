## 1. Employee board

- [x] 1.1 Add `employee_board` / `EmployeeBoard` (published team grid, contract, wishes, unavailabilities) and verify unknown id raises `UnknownEmployee` and cuisine without a cycle returns empty assignments
- [x] 1.2 Verify salle generate + wish shows the full published grid, `held` follows published souhait warnings, and an unpublished live cran is invisible

## 2. Guardrails

- [x] 2.1 Run `pytest` green without edits to `web/`, `api/`, `contracts/`, engine formulas, preview/fill, hydrate, or `employee_view`
